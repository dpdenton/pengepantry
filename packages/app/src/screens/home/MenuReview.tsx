import React from 'react';
import {AppText} from '@pengepantry/core/lib/components/app-text/AppText';

export const MenuReview = () => {
  return <AppText style={{padding: 25}}>Menu review</AppText>;
};
