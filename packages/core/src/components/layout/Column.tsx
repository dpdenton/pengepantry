import styled from '@emotion/native';
import {AppView} from 'components/layout/AppView';

export const Column = styled(AppView)`
  height: 100%;
`;

export const CenteredColumn = styled(Column)`
  align-items: center;
`;
