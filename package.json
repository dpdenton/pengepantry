{
  "name": "@penge-pantry/mono",
  "private": true,
  "scripts": {
    "reset": "find . -type dir -name node_modules | xargs rm -rf && rm -rf yarn.lock"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": []
  },
  "dependencies": {
    "typescript": "4.4.4",
    "react-native": "*"
  }
}
